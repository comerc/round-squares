## 1. Backend Implementation
- [x] 1.1 **Логика OTP**: Создать утилиту для генерации 6-значных кодов и In-Memory хранилище (`Map<username, { code, expiresAt }>`).
- [x] 1.2 **Модификация Логина (`POST /auth/login`)**: Добавить валидацию `email` (обязательное поле), генерацию OTP, сохранение в Map и логирование кода.
- [x] 1.3 **Ответ Логина**: Вместо токена возвращать статус `{ require2fa: true }`.
- [x] 1.4 **Эндпоинт Верификации (`POST /auth/2fa/verify`)**: Реализовать проверку кода из Map, выдачу JWT токенов при успехе и удаление кода.

## 2. Frontend Implementation
- [x] 2.1 **Форма входа**: Добавить обязательное поле ввода Email.
- [x] 2.2 **Обработка ответа**: При получении `require2fa: true` скрывать форму логина и показывать поле ввода OTP.
- [x] 2.3 **Верификация**: Реализовать отправку OTP кода на `/auth/2fa/verify` и обработку успешного входа (сохранение токена, редирект).
